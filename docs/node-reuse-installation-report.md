# 0.8.0 本机 Node 复用验证

验证日期：2026-09-24。

- [运行包 CI](https://github.com/renhao12356578/zotero-codex/actions/runs/35967285752)：macOS arm64/x64、Windows x64、Linux arm64/x64 构建成功，各平台通过 53 项测试；另有 Node 22.13.1 兼容性测试通过。
- [隔离 Zotero 宿主](node-reuse-host-result.json)：14/14。覆盖本机 Node 探测、只下载服务包、真实 ZIP 解压和进程执行、运行验证失败后自动下载完整包、托管配置迁移、离线复用、保留专用 Node、手动切换及设置页来源提示。下载传输使用本地测试包，其中一份注入外部 Node 验证失败以检验回退。
- [复用 Node 的 stdio 验证](node-reuse-stdio-result.json)：3/3。使用插件生成的命令和参数，列出 43 个工具、连接真实 Zotero 插件并读取上下文。
- [日常安装只读验证](node-reuse-daily-result.json)：8/8。通过 Zotero 10.0.4 插件管理器更新正式 XPI，插件从公开 Release 自动下载服务包，复用 nvm 中的 Node 25.8.1。新服务目录不含 Node，旧版专用 Node 保留；托管 Codex 配置自动迁移，其他设置与备份逐项相同。实际启动新命令后，43 个工具可用，插件和服务均为 0.8.0，当前论文上下文读取成功。没有写入用户笔记或条目。

实际下载中遇到一次网络流中断，通过设置页「准备 / 重试」重新下载后成功完成。失败期间旧运行组件及配置未被替换。日常更新后需重启 Codex，让客户端重新加载已迁移的命令。

正式 Release 包含 XPI、清单、五个平台的完整包和五个平台的服务包，共 12 个附件；10 个 ZIP 的文件名与大小已和清单核对。macOS arm64 服务包为 25,736,062 字节，完整包为 64,991,886 字节，下载量减少约 60%。

正式 XPI 的源码与发布提交逐文件核对，SHA256：`25c9dc64e595c6e9ccde321b4d67d70b96a710c35b6385d3ce035b715f83c545`。

自动检测在 Zotero 启动、准备/重试、连接 Codex 时进行；不在 Zotero 关闭期间持续监控系统 Node。兼容性要求为稳定版 Node 22.13+（22.x）或 24+，还需通过当前平台、服务依赖与 PDF 原生组件验证。复用本机 Node 不会修改该可执行文件或系统 PATH。

真实图形宿主覆盖 macOS；Windows/Linux 目前覆盖 CI 服务测试和运行包自检，尚未完成真实 Zotero 图形安装验收。
