# Zotero Codex MCP v0.6.1

调整 Zotero MCP 设置页的视觉样式，使其与 Better Notes 的原生设置页一致。

- 使用 Zotero 原生 `groupbox` 分区、标题、按钮和间距；移除横跨整页的状态白块。
- “状态已刷新”现在紧跟在“刷新状态”按钮右侧显示。
- 保留连接状态、自动捕获开关、清除上下文、复制配置、脱敏诊断等功能。

验证：40 项自动化测试、14 项真实 Zotero 设置页检查、4 项真实 MCP stdio 检查通过；800 × 600 隔离 Zotero 窗口无横向溢出，并检查了设置页上下滚动区域的实际渲染。
